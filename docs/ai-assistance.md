# AI assistance disclosure

The original course submission documented limited AI assistance. AI was used as a supporting tool and its suggestions were manually checked against the project schema, code, and assignment requirements.

Documented assistance included:

- Passing form data objects into existing Express API methods in `EditForm.jsx`.
- Making the form card responsive in `FormBase.css`.
- Formatting date values for API requests and display in `FormBase.jsx`.
- Styling a responsive navigation bar and highlighting the active route in `Navbar.jsx`.
- Mapping data objects into Material UI Data Grid columns in `Table.jsx`.
- Refreshing tables after mutations in `RoadTripPlaces.jsx` and `RoadTripRoutes.jsx`.
- Structuring SQL mode configuration in the DDL.
- Formatting stored procedures for MariaDB while preserving the existing schema and comments.
- Reviewing assignment requirements and checking final submission completeness.

The database model, project-specific SQL, application integration, testing decisions, and final acceptance of assisted suggestions remained the responsibility of the two project contributors.
